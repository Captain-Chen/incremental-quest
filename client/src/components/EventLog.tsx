import React from "react";

const EventLog = ({data}) => (
    <fieldset className="eventlog">
        <legend>Event Log</legend>
            <div>Placeholder</div>
    </fieldset>
);

export default EventLog;