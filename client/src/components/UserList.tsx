import React from "react";

const UserList = ({data}) => (
    <fieldset className="userList">
        <legend>※Users</legend>
            <div>Placeholder</div>
    </fieldset>
);

export default UserList;